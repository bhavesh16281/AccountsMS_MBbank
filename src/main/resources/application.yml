server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: ROOT
    password:
    driver-class-name: org.h2.Driver
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
