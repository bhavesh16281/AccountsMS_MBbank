server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: ROOT
    password:
    driver-class-name: org.h2.Driver
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
  config:
    import:
      - application_qa.yml
      - application_prod.yml
  profiles:
    active: "prod"
build:
  version: "3.0"

accounts:
  message: "Accounts microservice is active"
  contactDetails:
    name: "Muthyalu Bhavesh"
    email: "bhavesh16281@gmail.com"
    phone: "+91 8309828565"
